/**
 * Interface for Fib Implementations
 * 
 */

public interface FibImplementation {
    
    /** 
     * Calculate the fib
     * 
     * @param n the number
     * @return the fib for that number
     */
    public long fib(int n);
}